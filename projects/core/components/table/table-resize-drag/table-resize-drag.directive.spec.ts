import { TableResizeDragDirective } from './table-resize-drag.directive';

describe('TableResizeDragDirective', () => {
  it('should create an instance', () => {
    const directive = new TableResizeDragDirective();
    expect(directive).toBeTruthy();
  });
});
