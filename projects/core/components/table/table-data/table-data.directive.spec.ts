import { TableDataDirective } from './table-data.directive';

describe('TableDataDirective', () => {
  it('should create an instance', () => {
    const directive = new TableDataDirective();
    expect(directive).toBeTruthy();
  });
});
